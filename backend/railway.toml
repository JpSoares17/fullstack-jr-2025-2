[build]
builder = "dockerfile"

[deploy]
startCommand = "./start-no-db.sh"
healthcheckPath = "/docs"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
PORT = "8000"
